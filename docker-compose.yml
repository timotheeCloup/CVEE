services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: postgres-pgvector
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cvee_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  cvee-api:
    build: ./api
    container_name: cvee-api
    ports:
      - "8000:8080"
    volumes:
      - ./api:/app
    # Auto-reload on source changes (the bind-mount above shadows /app).
    command: uvicorn app:app --host 0.0.0.0 --port 8080 --reload
    # Read-only against the real Supabase database (same data as production),
    # mirroring the Cloud Run dev environment. The API only runs SELECTs.
    environment:
      DB_HOST: ${SB_HOST}
      DB_PORT: ${SB_PORT}
      DB_USER: ${SB_USER}
      DB_PASSWORD: ${SB_PASSWORD}
      DB_NAME: ${SB_NAME}
  cvee-ui:
    build: ./ui
    ports:
      - "8501:8080"
    volumes:
      - ./ui:/app
    environment:
      API_URL: http://cvee-api:8080/embed-cv
    depends_on:
      cvee-api:
        condition: service_started
        

volumes:
  postgres_data: